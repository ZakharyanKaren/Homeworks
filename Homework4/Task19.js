// Given an array of a size smaller than 100. It consists of numbers from 0 to 99 in any order. Create a new
// array where each element from that array is placed underthe index of its value. Start from the smallest
// value and end with the biggest one. If there are missing values, put in its places undefined.

// I DIDNT UNDERSTAND THIS TASK.

// let arr = [26, 30, 19, 5];
// let newArr = [];

// arr = arr.sort(function(a, b) {
//     return a - b;
//     });

// newArr.push(arr[0]);

// for(let i = 1; i < arr.length; i++) {
//     if(arr[i] > newArr.length) {

//     }
//     newArr.push(arr[i]);

// }
